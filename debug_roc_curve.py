import random
import pandas as pd

def form_random_sample(df, num=50):
    right_doc_type = []
    wrong_doc_type = []
    right_ = []
    wrong_ = []
    #TODO ...
    for i in range(len(df["Image path"])):
        if df["Is rejected"][i] == "False":
            right.append
    return 0
